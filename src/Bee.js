//Declare Bee as the subclass of Grub
class Bee extends Grub {
  //have a constructor method
  constructor() {
  	//Write the equivalent to 'Grub.call(this)'
  	super();
  	this.age = 5;
  	this.color = 'yellow';
  	this.job = 'Keep on growing';
  }

  //write other methods here.
};
